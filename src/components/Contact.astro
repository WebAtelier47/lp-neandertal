---
import Button from './Button.astro';
---

<section class="section contact" id="contact" aria-labelledby="contact__title">
  <div class="container contact__container">

    <div class="contact__block-header">
      <p class="contact__label">Nous contacter</p>
      <h2 class="contact__title" id="contact__title">Une question ?<br />On vous répond.</h2>
      <p class="contact__lead">
        Commande, devis, conseil sur les essences ou simple curiosité —
        écrivez-nous, nous répondons sous 24 h.
      </p>
    </div>

    <form
      class="contact__block-form"
      action="https://formspree.io/f/YOUR_FORM_ID"
      method="POST"
      novalidate
    >
      <div class="contact__form-row">
        <div class="contact__form-field">
          <label class="contact__form-label" for="contact-name">Nom</label>
          <input
            class="contact__form-input"
            type="text"
            id="contact-name"
            name="name"
            autocomplete="name"
            required
            placeholder="Votre nom"
          />
        </div>
        <div class="contact__form-field">
          <label class="contact__form-label" for="contact-email">Email</label>
          <input
            class="contact__form-input"
            type="email"
            id="contact-email"
            name="email"
            autocomplete="email"
            required
            placeholder="votre@email.com"
          />
        </div>
      </div>

      <div class="contact__form-field">
        <label class="contact__form-label" for="contact-subject">Sujet</label>
        <input
          class="contact__form-input"
          type="text"
          id="contact-subject"
          name="subject"
          placeholder="En quoi pouvons-nous vous aider ?"
        />
      </div>

      <div class="contact__form-field">
        <label class="contact__form-label" for="contact-message">Message</label>
        <textarea
          class="contact__form-input contact__form-textarea"
          id="contact-message"
          name="message"
          rows="5"
          required
          placeholder="Votre message…"
        ></textarea>
      </div>

      <div class="contact__form-footer">
        <Button type="submit">Envoyer le message</Button>
        <p class="contact__form-note">Réponse sous 24 h — Aucun démarchage commercial.</p>
      </div>
    </form>

  </div>
</section>

<style>
  .contact {
    padding-block: var(--space-section-lg);
    background-color: var(--color-bg-subtle);
  }

  .contact__container {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-gap-lg);
    align-items: start;
  }

  @media (min-width: 768px) {
    .contact__container {
      grid-template-columns: 1fr 1fr;
    }
  }

  /* Block: header */
  .contact__label {
    font-size: var(--label-size);
    font-weight: var(--label-weight);
    letter-spacing: var(--label-tracking);
    text-transform: var(--label-transform);
    color: var(--color-accent-text);
    margin-bottom: var(--space-stack-md);
  }

  .contact__title {
    font-family: var(--font-heading);
    font-size: var(--h2-size);
    line-height: var(--h2-leading);
    font-weight: var(--h2-weight);
    letter-spacing: var(--h2-tracking);
    color: var(--color-text);
    margin-bottom: var(--space-stack-md);
  }

  .contact__lead {
    font-size: var(--lead-size);
    line-height: var(--lead-leading);
    color: var(--color-text-secondary);
    max-width: 38ch;
  }

  /* Block: form */
  .contact__block-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-stack-md);
  }

  .contact__form-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-stack-md);
  }

  @media (min-width: 480px) {
    .contact__form-row { grid-template-columns: 1fr 1fr; }
  }

  .contact__form-field {
    display: flex;
    flex-direction: column;
    gap: var(--space-stack-xs);
  }

  .contact__form-label {
    font-size: var(--label-size);
    font-weight: var(--label-weight);
    letter-spacing: var(--label-tracking);
    text-transform: var(--label-transform);
    color: var(--color-text-secondary);
  }

  .contact__form-input {
    font-size: var(--text-base);
    line-height: var(--p-leading-normal);
    color: var(--color-text);
    background-color: var(--color-surface);
    border: var(--border-default);
    border-radius: var(--radius-sm);
    padding: var(--space-inline-sm) var(--space-inline-md);
    transition: var(--transition-colors), border-color var(--duration-normal) var(--ease-default);
    width: 100%;
    outline: none;
  }

  .contact__form-input::placeholder {
    color: var(--color-text-tertiary);
  }

  .contact__form-input:focus {
    border-color: var(--color-accent);
  }

  .contact__form-textarea {
    resize: vertical;
    min-height: 8rem;
  }

  .contact__form-footer {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--space-gap-sm);
  }

  .contact__form-note {
    font-size: var(--caption-size);
    color: var(--color-text-tertiary);
    letter-spacing: var(--caption-tracking);
  }
</style>
